/**
 * Заглушка. Нативный браузерный скролл (с CSS scroll-behavior: smooth для
 * якорей) работает плавнее любых JS-библиотек, особенно на мобилке.
 * Если захотите вернуть Lenis — попросите явно.
 */
export default function SmoothScroll() {
  return null;
}
